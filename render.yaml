services:
  - type: web
    name: network-connectivity-fintech
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir src app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
